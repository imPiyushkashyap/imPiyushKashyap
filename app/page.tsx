import HeroLandingPage from "@/components/ui/hero";
import HeroFooter from "@/components/ui/heroFooter";

const page = () => {
  return (
    <div>
      <HeroLandingPage />
      <HeroFooter />
    </div>
  );
};

export default page;
